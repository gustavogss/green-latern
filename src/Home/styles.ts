import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        padding: 30,
       alignItems: 'center'
    },
    title:{
        color: '#121212',
        fontSize: 20,
        fontWeight: '500'
    }
});

export default styles;